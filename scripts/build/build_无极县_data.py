#!/usr/bin/env python3
"""
石家庄市无极县领导班子工作关系网络 — 2026-07-15（⚠️ 数据待补充）
Build script generated by build_hebei_all_data.py

Generated from: 石家庄市 research data
Number of persons sourced: 0
"""

import sqlite3
import os

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
DB_PATH = os.path.join(BASE_DIR, "data/database/无极县_network.db")
GEXF_PATH = os.path.join(BASE_DIR, "data/graph/无极县_network.gexf")

# ── PERSONS ──
# (id, name, gender, ethnicity, birth, birthplace, education, party_join, work_start, current_post, current_org, source)
PERSONS = [
    ["无极县_sec_01", "（待确认）", "男", "汉族", "待查", "待查", "待查", "待查", "待查", "无极县委书记（待确认）", "中共石家庄市无极县委员会", "⚠️ 待确认：无极县暂无公开数据"],
]

# ── ORGANIZATIONS ──
# (id, name, type, level, parent, location)
ORGANIZATIONS = [
    ["无极县_party", "中共石家庄市无极县委员会", "党委", "县", "中共石家庄市委员会", "河北省石家庄市无极县"],
    ["无极县_gov", "无极县人民政府", "政府", "县", "石家庄市人民政府", "河北省石家庄市无极县"],
]

# ── POSITIONS ──
# (person_id, org_id, title, start, end, rank, note)
POSITIONS = [
]

# ── RELATIONSHIPS ──
# (person_a, person_b, type, context, overlap_org, overlap_period)
RELATIONSHIPS = [
]


# ── HELPERS ──

def esc(s):
    if s is None:
        return ""
    return str(s).replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;").replace('"', "&quot;").replace("'", "&apos;")


def person_color(p):
    name = p[0]
    role = p[9] or ""
    if "书记" in role and "纪委" not in role and "副书记" not in role:
        return "220,30,30"
    if "区长" in role or "县长" in role or "市长" in role:
        return "40,100,220"
    if "副区长" in role or "副县长" in role or "副市长" in role:
        return "40,140,220"
    if "纪委书记" in role:
        return "180,130,50"
    if "人大" in role or "政协" in role:
        return "220,160,40"
    if "副书记" in role:
        return "180,60,180"
    if "部长" in role or "政法委" in role:
        return "120,120,120"
    return "160,160,160"


def person_size(p):
    role = p[9] or ""
    if "区委书记" in role or "县委书记" in role or "市委书记" in role:
        return "20.0"
    if "区长" in role or "县长" in role or "市长" in role:
        return "18.0"
    if "副书记" in role:
        return "16.0"
    if "人大" in role or "政协" in role:
        return "14.0"
    return "12.0"


def org_color(o):
    t = o[2] or ""
    if "党委" in t: return "200,60,60"
    if "政府" in t or "公安" in t: return "60,100,200"
    if "人大" in t or "政协" in t: return "200,150,40"
    if "纪委" in t: return "160,120,40"
    return "120,120,120"


# ── BUILD DATABASE ──

def create_db():
    os.makedirs(os.path.dirname(DB_PATH), exist_ok=True)
    conn = sqlite3.connect(DB_PATH)
    c = conn.cursor()

    c.executescript("""
        CREATE TABLE IF NOT EXISTS persons (
            id TEXT PRIMARY KEY,
            name TEXT,
            gender TEXT,
            ethnicity TEXT,
            birth TEXT,
            birthplace TEXT,
            education TEXT,
            party_join TEXT,
            work_start TEXT,
            current_post TEXT,
            current_org TEXT,
            source TEXT
        );
        CREATE TABLE IF NOT EXISTS organizations (
            id TEXT PRIMARY KEY,
            name TEXT,
            type TEXT,
            level TEXT,
            parent TEXT,
            location TEXT
        );
        CREATE TABLE IF NOT EXISTS positions (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            person_id TEXT NOT NULL,
            org_id TEXT NOT NULL,
            title TEXT NOT NULL,
            start TEXT,
            end TEXT,
            rank TEXT,
            note TEXT,
            FOREIGN KEY (person_id) REFERENCES persons(id),
            FOREIGN KEY (org_id) REFERENCES organizations(id)
        );
        CREATE TABLE IF NOT EXISTS relationships (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            person_a TEXT NOT NULL,
            person_b TEXT NOT NULL,
            type TEXT NOT NULL,
            context TEXT,
            overlap_org TEXT,
            overlap_period TEXT,
            FOREIGN KEY (person_a) REFERENCES persons(id),
            FOREIGN KEY (person_b) REFERENCES persons(id)
        );
    """)

    for p in PERSONS:
        c.execute("""
            INSERT OR REPLACE INTO persons
            (id, name, gender, ethnicity, birth, birthplace, education,
             party_join, work_start, current_post, current_org, source)
            VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
        """, p)

    for o in ORGANIZATIONS:
        c.execute("""
            INSERT OR REPLACE INTO organizations
            (id, name, type, level, parent, location)
            VALUES (?, ?, ?, ?, ?, ?)
        """, o)

    for pos in POSITIONS:
        c.execute("""
            INSERT INTO positions
            (person_id, org_id, title, start, end, rank, note)
            VALUES (?, ?, ?, ?, ?, ?, ?)
        """, pos)

    for r in RELATIONSHIPS:
        c.execute("""
            INSERT INTO relationships
            (person_a, person_b, type, context, overlap_org, overlap_period)
            VALUES (?, ?, ?, ?, ?, ?)
        """, r)

    conn.commit()
    conn.close()
    print(f"\u2705 Database created: {DB_PATH}")


# ── BUILD GEXF ──

def generate_gexf():
    os.makedirs(os.path.dirname(GEXF_PATH), exist_ok=True)

    lines = []
    lines.append('<?xml version="1.0" encoding="UTF-8"?>')
    lines.append('<gexf xmlns="http://gexf.net/1.3"')
    lines.append('      xmlns:viz="http://gexf.net/1.3/viz"')
    lines.append('      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"')
    lines.append('      xsi:schemaLocation="http://gexf.net/1.3 http://gexf.net/1.3/gexf.xsd"')
    lines.append('      version="1.3">')
    lines.append('  <meta>')
    lines.append('    <creator>China-Gov-Network Investigation</creator>')
    lines.append(f'    <description>{esc("{parent_city}{region}领导班子工作关系网络")}</description>')
    lines.append(f'    <date>{today_str}</date>')
    lines.append('  </meta>')
    lines.append('  <graph mode="static" defaultedgetype="undirected">')

    # Node attributes
    lines.append('    <attributes class="node">')
    lines.append('      <attribute id="type" title="Node Type" type="string"/>')
    lines.append('      <attribute id="role" title="Role" type="string"/>')
    lines.append('      <attribute id="org_type" title="Org Type" type="string"/>')
    lines.append('    </attributes>')

    # Edge attributes
    lines.append('    <attributes class="edge">')
    lines.append('      <attribute id="type" title="Edge Type" type="string"/>')
    lines.append('      <attribute id="start" title="Start Date" type="string"/>')
    lines.append('      <attribute id="end" title="End Date" type="string"/>')
    lines.append('      <attribute id="rank" title="Rank" type="string"/>')
    lines.append('      <attribute id="strength" title="Strength" type="string"/>')
    lines.append('      <attribute id="context" title="Context" type="string"/>')
    lines.append('    </attributes>')

    # Nodes - Persons
    lines.append('    <nodes>')
    for p in PERSONS:
        pid = p[0]
        label = p[1]
        role = p[9] or ""
        birth = p[4] or ""
        birthplace = p[5] or ""
        c = person_color(p)
        sz = person_size(p)
        rgb = c.split(",")
        lines.append(f'      <node id="{{pid}}" label="{{esc(label)}}">')
        lines.append('        <attvalues>')
        lines.append('          <attvalue for="type" value="person"/>')
        lines.append(f'          <attvalue for="role" value="{{esc(role)}}"/>')
        lines.append(f'          <attvalue for="birth" value="{{esc(birth)}}"/>')
        lines.append(f'          <attvalue for="birthplace" value="{{esc(birthplace)}}"/>')
        lines.append('        </attvalues>')
        lines.append(f'        <viz:color r="{{rgb[0]}}" g="{{rgb[1]}}" b="{{rgb[2]}}" a="1.0"/>')
        lines.append(f'        <viz:size value="{{sz}}"/>')
        lines.append('      </node>')

    # Nodes - Organizations
    for o in ORGANIZATIONS:
        oid = o[0]
        label = o[1]
        c = org_color(o)
        rgb = c.split(",")
        lines.append(f'      <node id="{{oid}}" label="{{esc(label)}}">')
        lines.append('        <attvalues>')
        lines.append('          <attvalue for="type" value="org"/>')
        lines.append(f'          <attvalue for="org_type" value="{{esc(o[2])}}"/>')
        lines.append('        </attvalues>')
        lines.append(f'        <viz:color r="{{rgb[0]}}" g="{{rgb[1]}}" b="{{rgb[2]}}" a="1.0"/>')
        lines.append('        <viz:size value="8.0"/>')
        lines.append('        <viz:shape value="square"/>')
        lines.append('      </node>')

    lines.append('    </nodes>')

    # Edges
    eid = 0
    lines.append('    <edges>')
    for pos in POSITIONS:
        eid += 1
        pid, oid, title, start, end, rank, note = pos
        lines.append(f'      <edge id="e{{eid}}" source="{{pid}}" target="{{oid}}" type="directed" label="{{esc(title)}}">')
        lines.append('        <attvalues>')
        lines.append('          <attvalue for="type" value="worked_at"/>')
        lines.append(f'          <attvalue for="start" value="{{esc(start or "")}}"/>')
        lines.append(f'          <attvalue for="end" value="{{esc(end or "")}}"/>')
        lines.append(f'          <attvalue for="rank" value="{{esc(rank or "")}}"/>')
        lines.append('        </attvalues>')
        lines.append('        <viz:color r="80" g="80" b="80" a="0.5"/>')
        lines.append('        <viz:thickness value="1.0"/>')
        lines.append('      </edge>')

    for r in RELATIONSHIPS:
        eid += 1
        a, b, typ, context, overlap_org, overlap_period = r
        is_strong = "强关系" in str(typ)
        cr, cg_val, cb = (184, 149, 62) if is_strong else (91, 139, 192)
        thickness = 2.5 if is_strong else 1.5
        lines.append(f'      <edge id="e{{eid}}" source="{{a}}" target="{{b}}" type="undirected" label="{{esc(context)}}">')
        lines.append('        <attvalues>')
        lines.append('          <attvalue for="type" value="relationship"/>')
        lines.append(f'          <attvalue for="strength" value="{{esc(typ)}}"/>')
        lines.append(f'          <attvalue for="context" value="{{esc(context)}}"/>')
        lines.append('        </attvalues>')
        lines.append(f'        <viz:color r="{{cr}}" g="{{cg_val}}" b="{{cb}}" a="0.8"/>')
        lines.append(f'        <viz:thickness value="{{thickness}}"/>')
        lines.append('      </edge>')

    lines.append('    </edges>')
    lines.append('  </graph>')
    lines.append('</gexf>')

    with open(GEXF_PATH, "w", encoding="utf-8") as f:
        f.write("\n".join(lines))
    print(f"\u2705 GEXF graph created: {GEXF_PATH}")


# ── STATS ──

def print_stats():
    conn = sqlite3.connect(DB_PATH)
    c = conn.cursor()
    for table in ["persons", "organizations", "positions", "relationships"]:
        c.execute(f"SELECT COUNT(*) FROM {{table}}")
        cnt = c.fetchone()[0]
        print(f"  {{table}}: {{cnt}}")
        if table == "persons":
            c.execute("SELECT COUNT(*) FROM persons WHERE source LIKE '%待确认%'")
            pending = c.fetchone()[0]
            print(f"    └─ 待确认: {{pending}}, 已确认: {{cnt - pending}}")
    conn.close()


if __name__ == "__main__":
    print("=" * 60)
    print(f"  {parent_city}{region}领导班子工作关系网络")
    print(f"  等级: {{level}}")
    print("=" * 60)
    create_db()
    generate_gexf()
    print("\n\U0001f4ca Summary:")
    print_stats()
    print("\n\u2705 Done.")

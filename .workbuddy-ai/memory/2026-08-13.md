# 2026-08-13

## 根目录清理与归位（commit ea730711a, 07dace3df）
- 目标：清理 gov-relation 根目录游离文件。
- 完成：
  - 删除 34 个与 `data/persons/` 逐字节相同的重复人物 JSON + 1 个旧简版 `王平`（data/persons 有更全版本）。
  - 4 个唯一人物 JSON 归位到 `data/persons/`（顺城区、高奇英、李祥生、李涛）。
  - 根目录 `*_network.gexf/.db` 中文副本删除（data/graph 有权威版）；`zigong_network.{db,gexf}` git mv 到 `data/graph/`（原来缺）。
  - `research_notes.md`→`data/research/research_notes_思明区.md`；`test_shijiazhuang_research.json`→`data/research/`。
  - `todo_batch.sh`、`watchdog.sh` → `scripts/tools/`，同步 `docs/SYSTEM_OVERVIEW.md` 引用路径。
  - `git rm` 一次性临时：`task_context.md`、`checkpoint_02_artifacts.md`、0 字节 `]*`。
- 提交身份：仓库历史用 `谢明 <xieming@iauto.com>`（本地未设 git 全局 user，用 `-c user.name/email` 内联提交）。
- 根目录现只剩规范条目：AGENTS/README/CLAUDE、数据与代码目录、README 引用文件。

## 项目结构要点（供参考）
- 统一库约定在 `data/platform/gov_relation.db`，但扫描时**尚未生成**（需 `scripts/govdb.py build` 才会出现）；es 上才有 gov_relation.db。
- 大量 `<区>_network.db` 在 `data/` 与根目录扩散；其中 `data/graph/` 只含 gexf + 一个 quannan db。
## 复查结果（self review，commit ea730711a 后）
- git 工作区干净；分支领先 origin/main 2 个提交（未 push）。
- 数据保全验证通过：
  - 35 个被删 JSON 经 `git diff -z` 解码后 ALL 有 data/persons 正本（missing=0）。
  - 所有 git mv 均 100% rename，内容完整；移动后 gexf 为合法 XML，zigong.db 表结构完整。
  - 此前的 NO-BACKUP 为 shell 对中文路径八进制转义导致的误报。
- 遗留发现（非本次引入，未处理）：
  - `data/graph/quannan_network.db` 为 0 字节空文件，历史提交 cc2b0eb07 遗留下；data/graph 出现 .db 本身与生成器约定（数据入 data/database）不符。
  - `zigong` 已在 data/graph，但生成器会把 db 写 data/database（gitignore 忽略、需重建）。
- 待办：需 push 2 个本地提交。

## 固化缺口为 data/TODO.json
- 目标：把根目录清理后发现的 44 个缺口地区固化为队列数据源 data/TODO.json，使 todo_queue.py 恢复工作。
- 实际：剔除 4 个误报(chenzhou/jingdezhen-mayor/langya/栖霞市已有图) + 4 个聚合脚本(ganzhou_remaining/hebei_all/jiujiang_county_cities/nanchang)后，固化 **37 个单地区任务、15 省**。
- 每任务含 id/task_id/province/parent_city/region/level/targets/done。
- 生成脚本 /tmp/gen_todo.py（一次性，未入库）。
- 校验：gov_relation.todo.load_todo/iter_items/find_next/province_stats 全部通过；todo_queue.py status=0/37 done；claim 测试生成 dispatch prompt 后已 release（队列归零）。
- data/TODO.json 在 .gitignore 中（运行时文件，不含提交）。
- 备注：含聚合特性的 build 脚本(ganzhou_remaining/hebei_all/jiujiang_county_cities/nanchang)不应作为单任务派发。

## 项目约定备注（MEMORY.md 候选）
- 新队列格式：tasks 需含 province/parent_city/region/level/targets([{role}])/done。
- 任务 id 惯例 {province_slug}_{region}（如 henan_鹿邑县、inner_mongolia_兴安盟）。

## 队列批量执行 china-gov-network（已跑 2/37）
- 目标：逐个完成 data/TODO.json 的 37 个缺口任务。
- 已 DONE：inner_mongolia_兴安盟、inner_mongolia_东河区（2/37）。
- 标准流程（已验证可复用）：
  1. Phase0 检查 build 脚本是否存在且含研究数据。
  2. STAGING_DIR=data/tmp/<task_id> 运行 build 脚本 → 产 db/gexf（多数脚本自生成 person JSON）。
  3. scripts/process_tmp.py <stage> 干跑校验 → --apply(或 --apply --overwrite) promote 到 data/provinces/<slug>/。
  4. 写 report 到 stage → promote。
  5. 追加 report/open_gaps.md 条目。
  6. claim → done（claim 含 artifact 校验）。
- **关键 BUG 修复**：build_东河区_data.py 原用 parents[3] 定 REPO_ROOT，从 scripts/build 运行时解析错（写成 /Users/x/Desktop/data/tmp/... 污染仓库外）。已改为稳健 REPO_ROOT 探测 + 支持 STAGING_DIR 环境变量（参考 build_兴安盟_data.py 写法）。
- 部分脚本(兴安盟)不含自生成 person JSON，需用 scripts/tools/gen_person_json.py 生成（已建）。
- TODO 文件含 4 个聚合脚本已排除：ganzhou_remaining/hebei_all/jiujiang_county_cities/nanchang。

## 队列批量执行 china-gov-network（更新：已跑 5/37）
- 已 DONE：inner_mongolia_兴安盟/东河区/乌达区/满洲里市/玉泉区。
- 修复脚本：build_兴安盟(自带稳健bootstrap)、build_东河区(paren root+staging)、build_玉泉区(paren root+staging，import顺序需先sys.path再import)。build_乌达区/满洲里市自带_staging候选逻辑但需**先 mkdir 好 staging dir** 否则 fallback 到当前目录。
- 已提交4个commit（工具+脚本修复+各区域open_gaps）。
- 待办建议：剩下32个任务跨15省，主要在辽宁(6)/湖南(4)/吉林(4)/湖北(3)。
- 队列 done 前需 claim（done 要求 active claim）。
- 备注：scripts/build/ 下已积累大量历史遗留未归档 person JSON（李明东/李洪慈/李辉等，非本次造成），后续可考虑统一归位到 data/tmp 或 data/persons。盖州市书记链已重构(王庆珂→班耀康→吴峰→徐海龙→李典阁)。
